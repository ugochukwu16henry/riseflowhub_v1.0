# Backend API base URL (no trailing slash).
# - Local: backend must be running (e.g. pnpm dev in backend folder).
# - Production (e.g. Vercel): set to your deployed backend URL, e.g. https://api.riseflowhub.app
NEXT_PUBLIC_API_URL=http://localhost:4000

# Base URLs for each frontend surface (used for absolute links, redirects, metadata).
# For local dev you can keep everything on http://localhost:3000.
# In production, set per Vercel project:
# - Marketing:   https://riseflowhub.app
# - App:         https://app.riseflowhub.app
# - Investors:   https://investors.riseflowhub.app
# - Admin:       https://admin.riseflowhub.app
NEXT_PUBLIC_MAIN_SITE=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_INVESTOR_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3000

# Brand (overrides tenant name in dashboard sidebar and anywhere else this is read)
NEXT_PUBLIC_APP_NAME=RiseFlow Hub
NEXT_PUBLIC_BRAND=RiseFlow Hub
NEXT_PUBLIC_PLATFORM=RiseFlow Hub
