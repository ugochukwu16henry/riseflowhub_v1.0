# Database (required). On Railway: reference from Postgres plugin (Variable Reference).
DATABASE_URL=postgresql://user:password@localhost:5432/riseflowhub

# Server. Railway sets PORT automatically.
PORT=4000
FRONTEND_URL=http://localhost:3000

# Auth
JWT_SECRET=your-jwt-secret-min-32-chars

# --- Railway Storage Bucket (S3-compatible) ---
# When deploying on Railway, add a Bucket and link its credentials to this service.
# Railway injects: ENDPOINT, BUCKET, ACCESS_KEY_ID, SECRET_ACCESS_KEY, REGION
# If set, uploads use Railway bucket instead of Cloudinary.
# ENDPOINT=https://storage.railway.app
# BUCKET=your-bucket-xxxx
# ACCESS_KEY_ID=...
# SECRET_ACCESS_KEY=...
# REGION=auto

# --- Cloudinary (optional; used only if Railway bucket vars above are not set) ---
# CLOUDINARY_CLOUD_NAME=...
# CLOUDINARY_API_KEY=...
# CLOUDINARY_API_SECRET=...

# Paystack (live payments: setup fee, marketplace fee)
# Get keys from https://dashboard.paystack.com — use Live keys for production.
# Never commit real keys; set these in your host (e.g. Railway/Render) environment.
PAYSTACK_SECRET_KEY=YOUR_PAYSTACK_SECRET_KEY_HERE
PAYSTACK_PUBLIC_KEY=YOUR_PAYSTACK_PUBLIC_KEY_HERE
# PAYSTACK_WEBHOOK_URL=https://your-api.example.com/api/v1/webhooks/paystack

# Optional: Stripe (if not using Paystack)
# STRIPE_SECRET_KEY=...
# STRIPE_WEBHOOK_SECRET=...

# Email (receipts, notifications)
# SMTP_HOST=...
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=...
# SMTP_PASS=...
# EMAIL_FROM=RiseFlow Hub <noreply@riseflowhub.com>

# Optional: keep-alive and alerts (see docs/HEALTH_KEEPALIVE.md)
# SELF_URL=https://your-backend.up.railway.app
# ADMIN_EMAIL=admin@yourdomain.com
