# AfriLaunch Hub Backend - Environment Variables
# Copy to .env and fill in values

PORT=4000
NODE_ENV=development

# Database (PostgreSQL - use Supabase or local)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# JWT (required for auth)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (optional; default: http://localhost:3000)
# FRONTEND_URL=http://localhost:3000

# Optional: File storage (Phase 2+)
# CLOUDINARY_URL=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=

# Optional: AI (Phase 5+)
# OPENAI_API_KEY=
# EXCHANGE_RATE_API_KEY=

# Email (Nodemailer SMTP - works with SendGrid/Resend SMTP)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# EMAIL_FROM=AfriLaunch Hub <noreply@yourdomain.com>

# Optional: protect POST /api/v1/notifications/email (cron/internal)
# INTERNAL_API_KEY=your-internal-api-key
